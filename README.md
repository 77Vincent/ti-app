# ti-app

Minimal application scaffold.

## Development

- `make install`
- `make dev`

Open `http://localhost:3000` in your browser.

## Commands

- `make lint`
- `make test`
- `make build`
- `make start`
- `make check`
